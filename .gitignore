# Fetched by code/scripts/fetch_assets.sh (MMMU data must never be used for training; keep it out of git)
data/
models/

# Full model outputs embed the MMMU questions; commit the other files in results/<run>/
results/*/predictions.jsonl

logs/
__pycache__/
*.pyc
.pytest_cache/
*.part
